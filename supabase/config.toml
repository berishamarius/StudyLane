[project]
name = "study-lane"
organization_id = "YOUR_SUPABASE_ORG_ID"

[functions]
# Local function entry must point to built JavaScript or TypeScript sources.
# The Supabase CLI uses ./supabase/functions by default.

[auth]
auto_generate_sign_in_page = false
